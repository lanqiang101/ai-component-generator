name = "ai-component-proxy"
main = "index.js"
compatibility_date = "2024-01-01"

# 本地开发时的环境变量（生产环境在 Cloudflare Dashboard 配置）
[vars]
# ARK_API_KEY = "your-api-key-here"  # 不要在这里填写真实的 Key，使用 wrangler secret put ARK_API_KEY 命令
