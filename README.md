# ai-component-generator
 AI-powered frontend component generator. Configure via form, AI generates code for your chosen framework and UI library version, with real-time preview and live editing.  Supports React/Vue/HTML, configurable UI library versions &amp; CSS preprocessors, multiple resolution preview, works with local/cloud AI models, config stored in browser
